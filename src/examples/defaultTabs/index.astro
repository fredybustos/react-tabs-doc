---
import DefaultTabs from ".";
const [template] = await Astro.glob("../markdown/default.md");
const content = template.compiledContent();
---

<h2 class="text-xl font-semibold dark:text-white mb-6">Default tabs</h2>
<DefaultTabs client:load content={content} />
